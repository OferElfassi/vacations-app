const vacationListStyle = theme => ({
  root: {
    marginTop: 200,
    color: theme.palette.primary.lighter,
    textShadow: '-3px 4px 8px rgba(0,0,0,0.31)',
  },
});

export default vacationListStyle;
